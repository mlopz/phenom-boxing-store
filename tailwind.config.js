/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        'phenom-red': '#DC2626',
        'phenom-dark': '#1F2937',
        'phenom-black': '#111827',
      },
      fontFamily: {
        'aggressive': ['Impact', 'Arial Black', 'sans-serif'],
      },
      animation: {
        'pulse-slow': 'pulse 3s cubic-bezier(0.4, 0, 0.6, 1) infinite',
      }
    },
  },
  plugins: [],
}
